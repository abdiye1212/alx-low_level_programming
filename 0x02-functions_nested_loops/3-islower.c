#include "main.h"
/**
 * _islower - chacks for lowercase character
 * @c: parameter to be chacked
 * Return: 1 or 0
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (0);
	else
		return (0);
}
